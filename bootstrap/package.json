{
    "main": "./js/toast.js",
    "scripts": {
        "build": "tsc",
        "watchBuild": "tsc -w",
        "compile": "npm run build && node . ",
        "watchCompile": "nodemon --exec 'npm run build && node .' --watch ./ts -e ts --watch"
    },
    "devDependencies": {
        "nodemon": "^2.0.15",
        "tslib": "^2.3.1",
        "typescript": "^4.4.4"
    }
}