{
    "main": "./js/toast.js",
    "scripts": {
        "build": "tsc",
        "watchBuild": "tsc -w",

        "toast": "npm -s run build; node .",
        "toastWatch": "nodemon -x 'npm run toast -s' -e toast,ts"
    },
    "devDependencies": {
        "nodemon": "^2.0.15",
        "tslib": "^2.3.1",
        "typescript": "^4.4.4"
    }
}